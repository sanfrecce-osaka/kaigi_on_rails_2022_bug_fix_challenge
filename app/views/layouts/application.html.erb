<!DOCTYPE html>
<html>
  <head>
    <title>KaigiOnRails2022BugFixChallenge</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-dark bg-dark">
      <div class="container-fluid d-flex justify-content-between">
        <a class="navbar-brand" href="#">Kaigi On Rails 2022 Bug Fix Challenge</a>
        <% if account_signed_in? %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <%= link_to 'ログアウト', [:destroy, :account, :session], class: 'nav-link active', 'aria-current': 'page', data: { turbo_method: :delete } %>
            </li>
          </ul>
        <% end %>
      </div>
    </nav>
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
    <%= yield %>
  </body>
</html>
