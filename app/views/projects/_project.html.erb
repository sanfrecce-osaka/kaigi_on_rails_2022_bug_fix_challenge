<div id="<%= dom_id project %>">
  <p>
    <strong>Name:</strong>
    <%= project.name %>
  </p>
  <ul>
    <% project.tasks.each do |task| %>
      <li><%= link_to task.name, task %></li>
    <% end %>
  </ul>
</div>
